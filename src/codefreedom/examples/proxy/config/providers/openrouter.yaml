---
# ── OpenRouter Provider ────────────────────────────────────────────────────────
# Routes models through OpenRouter (openrouter.ai) — aggregates hundreds of
# models from multiple providers, including free-tier options.
#
# Use this for:
#   - Accessing models not available through direct provider APIs
#   - Free-tier models (e.g., Nemotron, Groq via OpenRouter)
#   - Automatic model routing via OpenRouter's "free" endpoint
#
# ── HOW TO CONFIGURE ───────────────────────────────────────────────────────
# Set these in your .env or .env.secrets:
#
#   OPENROUTER_BASE_URL=https://openrouter.ai/api/v1
#   OPENROUTER_API_KEY=sk-or-your-key
#
# Model names use the openrouter/ prefix with the full provider/model path.
# To add more models, copy the block below and adjust model_name and model.
# ──────────────────────────────────────────────────────────────────────────

# model_list:
#   - model_name: OpenRouter/Nemotron-3-Ultra-550B-A55B
#     litellm_params: &openrouter_params
#       model: openrouter/nvidia/nemotron-3-ultra-550b-a55b:free
#       api_base: os.environ/OPENROUTER_BASE_URL
#       api_key: os.environ/OPENROUTER_API_KEY
#       timeout: 300
#       drop_params: true
#       extra_body:
#         stream_options:
#           include_usage: true

#     model_info: &openrouter_model_info
#       id: "openrouter-nemotron-3-ultra-550b-a55b"
#       mode: chat
#       supports_system_messages: true
#       supports_native_streaming: true
#       supports_vision: true
#       supported_openai_params: &openrouter_supported_openai_params
#         - tools
#         - tool_choice
#         - parallel_tool_calls
#         - response_format
#         - max_tokens
#         - max_completion_tokens
#         - stream
#         - stream_options
#         - temperature
#         - top_p
#         - stop
#         - reasoning_effort

#   - model_name: OpenRouter/FreeRouter
#     litellm_params:
#       <<: *openrouter_params
#       model: openrouter/openrouter/free

#     model_info:
#       <<: *openrouter_model_info
#       id: "openrouter-freerouter"
#       # FreeRouter is a dynamic load-balanced endpoint with no native
#       # reasoning_effort gradient — drop that param to match the
#       # "only none" capability advertised to VS Code.
#       supported_openai_params: &openrouter_freerouter_supported_openai_params
#         - tools
#         - tool_choice
#         - parallel_tool_calls
#         - response_format
#         - max_tokens
#         - max_completion_tokens
#         - stream
#         - stream_options
#         - temperature
#         - top_p
#         - stop
